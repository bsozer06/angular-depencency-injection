import { ILayerService } from "../interfaces/ILayerService";

export abstract class AbstractLayerService implements ILayerService {
  prop_layerA: any;
  func_layerA(): any { };
  func_layerB(): any { };
}
